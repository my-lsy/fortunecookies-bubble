<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Your Fortune</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html, body {
      width: 100%;
      height: 100%;
      background-color: #0b0b0b;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .video-wrapper {
      position: relative;
      width: 100vw;
      height: 100vh;
      max-width: 480px; /* Constrain for larger screens/tablets */
      display: flex;
      justify-content: center;
      align-items: center;
    }
    video {
      width: 100%;
      height: 100%;
      object-fit: cover; /* Fills mobile screen neatly without distortion */
      display: block;
    }
  </style>
</head>
<body>

  <div class="video-wrapper">
    <!-- Critical mobile autoplay attributes: autoplay, muted, playsinline -->
    <video id="fortuneVideo" autoplay muted playsinline preload="auto">
      <source src="cookie.mp4" type="video/mp4">
      Your browser does not support HTML video.
    </video>
  </div>

  <script>
    // Safeguard: Force video playback on page load if browser hangs
    const video = document.getElementById('fortuneVideo');
    window.addEventListener('DOMContentLoaded', () => {
      video.play().catch(error => {
        console.log("Autoplay check:", error);
      });
    });

    // Hold last frame when the cookie finishes breaking
    video.addEventListener('ended', () => {
      video.pause();
    });
  </script>
</body>
</html>